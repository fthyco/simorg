[package]
name = "org_engine_replica"
version = "1.1.0"
edition = "2021"
description = "Deterministic Rust replica of OrgEngine v1.1"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
sha2 = "0.10"
